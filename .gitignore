# 의존성 모듈 (절대 올리지 않음)
node_modules/
*/node_modules/

# 환경 변수 (비밀번호 포함 - 절대 올리지 않음)
.env
.DS_Store

# 빌드 결과물
dist/
build/
coverage/

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 쿠버네티스 설정 파일 (DB 비밀번호 포함 - 필수 제외)
k8s.yaml